script.Parent.Touched:connect(function(hit)
	if hit.Parent:FindFirstChild("Humanoid") then
		Winner = Instance.new("Message", workspace)
	    Winner.Text = "Игра окончена"
	    wait(2)
		Winner:remove(2)
	end
end)
